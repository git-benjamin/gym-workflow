# Expo public env vars are inlined at build time and exposed to JS.
# Override the API URL when running on a real device (use the laptop's LAN IP).
EXPO_PUBLIC_API_URL=http://localhost:3000

# Bearer token, must match API_TOKEN on the api server.
EXPO_PUBLIC_API_TOKEN=
